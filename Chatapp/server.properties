# MySQL on your PC (localhost). Default XAMPP settings:
db.url=jdbc:mysql://localhost:3306/chatapp?useSSL=false&allowPublicKeyRetrieval=true
db.user=root
db.password=

# Chat server port (client connects here)
server.port=5000
